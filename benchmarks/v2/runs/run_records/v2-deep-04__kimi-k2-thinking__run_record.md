# Run Record Template

- `task_id`: 
- `task_title`:
- `model_id_used`:
- `run_date`:
- `thinking_mode_used`:
- `prompt_version`:
- `context_files_shared`:
- `raw_output_path`:
- `rate_limit_or_refusal_notes`:
- `latency_notes`:
- `manual_observations`:

## First-Pass Output Summary

Again i will stress how rapid it is in responses i cant even read as quick or follow the generated text. We dont have a scoring for this category but i would make a note of how quick it is, both in thinking and then output.

## Operational Notes

- Did the model appear to understand the codebase shape?
- Did it truncate, refuse, or drift?
- Did it require a larger-than-expected amount of context steering?