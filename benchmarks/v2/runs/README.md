# Runs Folder

Use this folder during manual execution.

- `raw_outputs/`
  Store one first-pass raw output file per model-task pair.

- `run_records/`
  Store one filled run record per model-task pair.

Recommended source of truth for the process:
- `../RUNBOOK.md`
